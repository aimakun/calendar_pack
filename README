Note:
- Replace existing modules (contribute version) are required.
- The qtip module require qtip library, place it in sites/all/libraries before use (this pack contains library in libraries directory (this directory is used for seperate qtip library only).
